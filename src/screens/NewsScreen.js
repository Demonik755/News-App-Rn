import React, { } from 'react';
import { StyleSheet, Text, View } from 'react-native';
export const NewsScreen =(news) => {
    return (
        <View>
            <Text>
                MainScreen
            </Text>
        </View>
    );
};

const style = StyleSheet.create({

});